/**
 * API Configuration Template
 *
 * INSTRUCTIONS:
 * 1. Copy this file to create your local `config.js`:
 *    cp scripts/config.example.js scripts/config.js
 *
 * 2. Edit `scripts/config.js` and replace the placeholder values with Real API keys
 *
 * TO GET API KEYS:
 *look into comments section of my project submission.
 */

const API_CONFIG = {
  // RapidAPI Key for Travel Advisor
  rapidApi: {
    key: "RAPIDAPI_KEY_HERE",
    host: "HOST_HERE",
  },
};

// NOTE: This file is a safe example and contains no secrets.
window.API_CONFIG = API_CONFIG;
